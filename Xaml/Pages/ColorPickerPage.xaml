<local:FramelessPageWithMessageDialog
    x:Class="TranslucentTB.Xaml.Pages.ColorPickerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:controls="using:TranslucentTB.Xaml.Controls"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TranslucentTB.Xaml.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" Width="350" IsClosable="True">

    <local:FramelessPage.TitlebarContent>
        <controls:ChromeButton ToolTipService.ToolTip="Always on top" IsTogglable="True" IsChecked="{x:Bind AlwaysOnTop, Mode = TwoWay}">
            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE840;" FontSize="10" FontWeight="Medium" />
        </controls:ChromeButton>
    </local:FramelessPage.TitlebarContent>

    <StackPanel Spacing="10">
        <ColorPicker x:Name="Picker" IsAlphaEnabled="True" IsMoreButtonVisible="True" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="10">
            <Button Width="100" Style="{ThemeResource AccentButtonStyle}">OK</Button>
            <Button Width="100">Cancel</Button>
        </StackPanel>
    </StackPanel>
</local:FramelessPageWithMessageDialog>
