<host:XamlApplication
    x:Class="TranslucentTB.Xaml.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TranslucentTB.Xaml"
    xmlns:host="using:Microsoft.Toolkit.Win32.UI.XamlHost"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <host:XamlApplication.Resources>
        <ResourceDictionary>
            <x:String x:Key="AppName">TranslucentTB</x:String>

            <Style x:Key="ActionsListViewStyle" TargetType="ListView">
                <Setter Property="SelectionMode" Value="None" />
                <Setter Property="IsItemClickEnabled" Value="True" />
                <Setter Property="ItemContainerStyle">
                    <Setter.Value>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="0,0,12,0"/>
                        </Style>
                    </Setter.Value>
                </Setter>
                <Setter Property="ItemTemplate" Value="{StaticResource ActionsListViewTemplate}" />
            </Style>

            <ResourceDictionary.MergedDictionaries>
                <local:BoundResources />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </host:XamlApplication.Resources>

</host:XamlApplication>
