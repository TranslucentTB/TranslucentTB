<ResourceDictionary
    x:Class="TranslucentTB.Xaml.BoundResources"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TranslucentTB.Xaml"
    xmlns:models="using:TranslucentTB.Xaml.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <DataTemplate x:Key="ActionsListViewTemplate" x:DataType="models:ActionItem">
        <Grid HorizontalAlignment="Left">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="48" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <FontIcon FontFamily="Segoe MDL2 Assets" FontSize="24" Glyph="{x:Bind Icon, Mode = OneWay}" Margin="0,15,0,0" VerticalAlignment="Top" />
            <StackPanel Margin="0,11,0,13" Grid.Column="1">
                <TextBlock FontSize="14" Text="{x:Bind Name, Mode = OneWay}"/>
                <TextBlock FontSize="13" LineHeight="16" Opacity="0.6" Text="{x:Bind Description, Mode = OneWay}" />
            </StackPanel>
        </Grid>
    </DataTemplate>
</ResourceDictionary>
